## 记录 20：O-4 认知工艺触发接线验证 + Mind 适配器零硬编码收口（2026-09-06，ADR-0022）
**健康快照**：✅ 完成（183 tests 全绿 = 182 + T3 驾驶模式不触发）
**物理事实**：
- 触发链代码早已存在（ADR-0001），但从未跑通真实 gRPC——探查确认既有
  mock Mind（mind_integration.rs）+ p11b 闭环已覆盖 adapter 层与 run_cycle 层，
  不重复建设（common/mod.rs 临时 MockMind 已 git checkout 回滚）
- mind.rs 探查抓到 12 处无来源字面量（EnergyContext 数值/阈值/探针回退/关键词数组）
  → MindConfig（[anaphase.mind] 可覆盖）单一来源收口，grep 仅剩
  heliotropism=0.0（注释声明派生来源：P10a 未实现中性值）
- `GrpcMindAdapter::new(endpoint, config)` 签名变更（8 处调用点同步）
**验证**：
- T3：Drive 装配（Noop）下 mock Mind 计数不变（零运行时分支，模式门=装配）✅
- 全量 183 passed / 0 failed ✅
- 0 硬编码：mind.rs 无字面量残留（grep 核对）✅
**状态**：✅ 完成（commit 见 git log；ECOSYSTEM v1.39 同步）
